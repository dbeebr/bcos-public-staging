---
bcos_type: index
kind: context
surface: project
id: CONTEXT-INDEX
title: "Teamcell Lite Context Index"
status: active
created: 2026-06-24
created_by: codex
created_by_type: agent
created_by_id: agent:codex
created_by_display: "Codex"
created_by_github: null
on_behalf_of_human_id: "human:{{CELL_OWNER_ID}}"
agent_id: agent:codex
agent_model: gpt-5
agent_capability_class: local-worker
---

# Teamcell Lite Context Index

Use this index to load only the context needed for the current work.

## Core Files

| File | Purpose |
|---|---|
| `README.md` | First entry point |
| `PROJECT.md` | Project purpose, scope, data policy, authority, and trust model |
| `TEAM-PROFILE.md` | Team roles and operating agreement |
| `AGENTS.md` | Agent rules, Fetch/Read Gate, and Completion Gate |
| `.github/copilot-instructions.md` | GitHub Copilot rules and gate adapter |
| `.bcos/CELL-PROFILE.yaml` | Cell profile: session entry order, compact routing and where procedures live — the Cell-specific facts rendered instruction blocks are built from |
| `.bcos/CELL-GOVERNANCE.yaml.template` | Governance-profile + role-based ownership config; copy to `.bcos/CELL-GOVERNANCE.yaml` and select a profile when ready — unresolved by default, never pre-selected |
| `scripts/start-cell.sh` | Daily start: identity + Git state, validation, active work item and whether it is ready for execution, next action |
| `scripts/validate-cell.sh` | Local validator; `--baseline <file>` compares failures by exact identity |
| `scripts/validate-completion.py` | Honest-completion and status-synchronicity check, called by the validator; `--ready <task>` is the format check a task must pass before execution (`--fix` only for rule-derivable technical fields) |
| `scripts/render-instructions.py` | Renders instruction blocks, context packs (planning packs carry the current task template; executor packs require a ready task) and the Procedure Index below from one shared core (`templates/PROJECT-INSTRUCTIONS.template.md`) with budget checks; `check` is called by the validator |
| `scripts/personalize-cell.py` | Non-interactive, idempotent personalization from verified facts (`plan`, `check`, `apply`): renders `instructions/`, fills the install-time slots of `ONBOARDING.md`, links the ready instructions from `README.md` and `ONBOARDING.md`, records what is still unresolved; reports a conflict instead of overwriting a hand-edited output |
| `packages/PACKAGES.yaml` | Package contract: baseline, optional Plus profile, optional Team Capability Pack |
| `.installation/TEAMCELL-INSTALLATION-RECEIPT.yaml` | Installed Cells only: source, version, packages and files this Cell received |
| `templates/` | Personalization sources: `PROJECT-INSTRUCTIONS.template.md` is the one shared instruction core; `APP-INSTRUCTIONS-*.template.md` are thin activation notes; `FIRST-RUN-AGENT-PROMPT.template.md` |
| `instructions/` | Generated by the installer's personalization step (`scripts/personalize-cell.py`): the ready-to-paste project block, one activation note per app and `PERSONALIZATION-MANIFEST.json` (the required outputs and their hashes) — never edited by hand; the validator fails on a missing required output, on hand edits and on unresolved tokens, and warns when stale |
| `human-gates/TEMPLATE.visual-human-gate.html` | Optional visual (non-authoritative) rendering of a human gate; the Markdown gate always wins on disagreement |
| `playbooks/onboarding/START-HERE.html` | Current primary onboarding page — generated fresh by `scripts/start-cell.sh` on every local start (stage, governance, active work, next action); not shipped by this template, appears after first `start-cell.sh` run |
| `playbooks/onboarding/TEAMCELL-LITE-INTRO.html` | Generic reference/workshop deck (up to ~90 min walkthrough) — not this Cell's current onboarding page, see `START-HERE.html` |
| `playbooks/onboarding/SKILL-PLAYGROUND.html` | Team Capability Pack skill playground — try Business Analyst, Requirements Engineering, and Team Event Planner in under 60 seconds. Installed only with the optional `team-capability-pack` package (`packages/PACKAGES.yaml`); absent from a baseline-only Cell |

## Identity Context

Use `TEAM-PROFILE.md` as the participant registry. Work, human-gate, playbook,
and verification-report artifacts must carry explicit actor identity fields,
including the accountable human when an agent writes on behalf of the team.

## Surfaces

| Surface | Path | Purpose |
|---|---|---|
| inbox | `inbox/INBOX.md` | Append-only raw intake |
| work | `work/` | Executable tasks |
| human-gates | `human-gates/` | Decisions requiring human review |
| history | `history/` | Decisions, records, reports, and reviews |
| playbooks | `playbooks/` | Reusable ways of working; the available ones are listed in the Procedure Index below |

## Procedure Index

Available procedures (skills, playbooks, workflows) in this Cell. Check this
list when a task or sub-task starts, at a handover, at a phase change and
when a new finding appears; load the full file of a matching entry before
applying it and record its use (`AGENTS.md` "Procedures"). The section is
generated from each procedure file's own frontmatter — refresh it with
`python3 scripts/render-instructions.py index --write` after a procedure is
accepted, changed or removed or a package is installed.

<!-- procedure-index:start — generated by scripts/render-instructions.py index --write from the procedure files' frontmatter; do not edit rows by hand -->
index_version: 88487657feb5 (2 available procedure(s))

| ID | Kind | Use when | Not when | Phase | Path | Package |
|---|---|---|---|---|---|---|
| AGENT-ROUTING | playbook | Before creating an artifact, folder, config or agent-instruction surface, or before a status, done or next-step claim | Editing inside an artifact whose place is already settled | plan, execute, close | `playbooks/AGENT-ROUTING.playbook.md` | teamcell-lite-baseline |
| PLAYBOOK-VALIDATOR-BASELINE | playbook | validate-cell.sh already fails before your change and you must prove you added no new failure | A fresh Cell or the template fails, or the failure is yours: fix it instead | execute, close | `playbooks/VALIDATOR-BASELINE.playbook.md` | teamcell-lite-baseline |
<!-- procedure-index:end -->

## Artifact Routing

Use this table before creating files.

Before any status, done, next-step, completion, or governance claim, apply the
Fetch/Read Gate in `AGENTS.md`: identify repo, branch, and relevant artifact;
read that artifact; distinguish user-reported status from Git-verified status;
load only relevant files; and route the next action below or to explicit `none`.

| Input or intent | Target |
|---|---|
| Raw unprocessed input | Append to `inbox/INBOX.md` |
| Executable work with outcome, gates, and validation | Create a task in `work/` |
| Human decision required before safe implementation | Create a gate in `human-gates/` |
| Durable evidence, review, report, or accepted decision | Record in `history/` |
| Reusable procedure or repeatable agent workflow | Create a playbook in `playbooks/` |
| Pre-existing validator failure you must not hide | Baseline by identity per `playbooks/VALIDATOR-BASELINE.playbook.md`; repair routed to `work/` |
| No actionable follow-up | Explicit `none` |
| Domain knowledge concept | First create a work item in `work/`; do not create a new knowledge or context surface until a human gate accepts the target placement |
| Active maintained domain capability (after gate) | `apps/<app-id>/` — see `docs/EXPANSION-PROFILE.md` |
| Repo-level agent behavior | Update `AGENTS.md` and, when allowed by the task, `.github/copilot-instructions.md` |
| Tool-specific or domain-specific agent guidance | Create a playbook first; promote later only after review |
| MCP configuration | Create a human gate first; include no secrets and assume no runtime until accepted |

A domain brain (for example, a brand brain) is domain knowledge work first: it
must be captured as a work item in `work/`. Its later target surface requires
the decision owner's human gate before implementation — an actively queried
capability is promoted to `apps/<app-id>/`; passive proven patterns are
distilled to `library/`. `docs/EXPANSION-PROFILE.md` records the rule and the
optional expansion surfaces (`decisions/`, `reports/`, `proofs/`,
`handoffs/`, `library/`, `apps/`).

## Trust Rule

Git evidence beats chat claims. If files disagree, preserve the disagreement in
a work item or human gate and ask for a human decision.
